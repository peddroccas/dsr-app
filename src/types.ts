export type user = {
  id: string
  name: string
  email: string
}

export type store = {
  id: string
  name: string
}

export type tasks = {
  id: string
  title: string
  weeklyFrequency: number
}
